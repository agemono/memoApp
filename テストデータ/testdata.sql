CREATE TABLE user_info(
    user_id CHAR(50),
    user_password(50)
);
INSERT INTO user_info(
    VALUES(testuser,logintest)
);

CREATE TABLE user_memo(
    user_id CHAR(50),
    memo_item CHAR(200)
);
